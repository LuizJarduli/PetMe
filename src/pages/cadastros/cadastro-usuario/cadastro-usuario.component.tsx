import { Component, ReactNode } from 'react';

export class CadastroUsuarioComponent extends Component {
    render(): ReactNode {
        return(
            <h1>Aqui vai a tela de cadastro</h1>
        );
    }
}